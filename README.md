# Static Schema Definitions

Schema sources live under `src/<version>/`. Build output copies the latest version **per
schema file** to `public/` (stable URLs) and every version to `public/<version>/`.
